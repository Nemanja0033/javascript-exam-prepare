/*var cars = ["Ford", "Mazda"]; 
cars[1] = "BMW"; 
console.log(cars[0]); 
console.log(cars[1]);

var numbers = [1, 2, 55, 23, 0, 11, 123, -33]; 
numbers.sort(); 
document.write(numbers.toString());*/

var shows = ['Sopranos', 'Fargo', 'Dexter', 'Breaking Bad'];
var movies = ['Godfather', 'Once Upon Time In America'];

shows.push('Narcos');
shows.pop();
shows.shift();
shows.unshift('Better Call Saul')


console.log(shows);